# Package

version     = "0.1.0"
author      = "genotrance"
description = "Fed Ep with UDitors"
license     = "MIT"

bin = @["feud"]

# Dependencies

requires "nim >= 0.19.0", "nimscintilla >= 0.2.0", "winim >= 2.5.2", "cligen >= 0.9.17"
